Album name: <%=@album.name%>
Photo: <%=@photos.count%>
<div class="row">
	<% @photos.each do |photo| %>
		<div class="col-md-3">
			<%=link_to (image_tag photo.image,size: "200x200"),photo %>
		</div>
	<% end %>
</div>

