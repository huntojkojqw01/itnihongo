<h1>New Photo</h1>
<% if @albums %>
	<%= render 'form', photo: @photo, albums: @albums%>
<% else %>
	Pet
	<%= select_tag :pet,options_from_collection_for_select(current_user.pets,:id,:name)%>
	<%=link_to 'new_pet',new_pet_path%>
<% end %>
<%= link_to 'Back', photos_path %>
