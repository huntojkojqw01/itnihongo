var tmp=parseInt($("#like_numbers").text())-1;
$("#like_numbers").text(tmp);
$("#dislike").replaceWith("<%= j link_to '',likes_path(user_id: current_user.id,photo_id: @photo.id),method: :post,remote: true,class: 'glyphicon glyphicon-thumbs-up',id: 'like' %>");
